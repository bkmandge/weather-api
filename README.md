# Django Weather API

This project is a basic Django REST API for managing weather data, cities, and saved cities.

## Features
- Add/Search Cities
- Add/List Weather data
- Save favorite cities
- No login required (for now)

## Setup
- `pip install -r requirements.txt`
- `python manage.py migrate`
- `python manage.py runserver`
